#include "common.h"

using std::string;
using std::chrono::system_clock;
using time_point = system_clock::time_point;

auto process_ui_file(string ui_write_file_address, time_point last_ui_timestamp) {


    return;
}